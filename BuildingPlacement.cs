using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BuildingPlacement : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // Called when the mouse is clicked over this collider
    void OnMouseDown()
    {
        Debug.Log("Building spot clicked");
        // Implement functionality to handle building placement here
    }
}
